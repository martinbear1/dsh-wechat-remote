# v1.7.1 · 本轮用量与用时

- 配合新版小程序，查看每轮回复的用量、耗时、首字延迟和输出速度；未记录的数据不显示。
- 改善旧插件首次升级的安装握手等待，减少因启动较慢而提前退出的问题。
- 安装和更新继续自动备份，保留已有配对与会话，无需另外安装 pnpm 或 Git。

```sh
npx -y dsh-wechat-remote@latest
```

请先结束运行中的任务。支持 DSH `0.1.1-rc.2` / `0.1.2-rc.1` 的 Windows、macOS Intel、Linux x64；不会修改 DSH 本体版本。旧版 1.5.5 用户执行上面的命令升级一次，此后可使用 WebUI 检查更新。

安全局域网需搭配相应新版小程序；旧小程序可通过已有公网配对继续使用。异常更新的备份保留在本机，不要删除节点或会话来处理安装问题。
