# v1.7.5

- 修复小程序回答问题、处理审批后，卡片仍停留在会话末尾或重连后再次出现的问题。
- 改善多端同时操作及网络重连时的问答、审批状态同步。
- 插件与 npm 安装工具统一使用 1.7.5 版本号，保留此前的安装及慢启动优化。

可在 WebUI 检查更新，或运行 `npx -y dsh-wechat-remote@latest`。升级前请结束运行中的任务；升级会重启 DSH，保留已有配对与会话，不改变 DSH 本体版本。
