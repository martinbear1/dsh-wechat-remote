# 鲸常在 DSH 插件 v1.6.0-rc.5

2026-09-08，Pre-release，仅主动预览，不合并 main、不替换 Latest v1.5.5。

## RC5：网络恢复与安全局域网

- 基线为 RC4 `b7aa41121fe61778fd1df1a4b6246f9e7d92e814`；本轮修复提交 `561aa44fdab6ea2f7fe267bde7a0bb07768e46fe`。保留 RC4 的多 DSH / 三系统适配和受控 WebUI 更新功能。
- LAN 与公网复用同一 DSH 隧道和端到端加密实现。LAN 在校验已配对的主机公钥后，才在加密通道中提交凭据；认证前不允许访问 DSH。不新增互联网监听端口、不关闭 DSH WebUI 认证。
- 新增 `harness.secure-lan` 能力及 `secureLan: 1` 引导信息；限制连接数、流数量、帧大小、缓冲区和认证等待时间。
- 身份撤销 / 重新注册时，退休旧连接和加密上下文，重连使用新身份；并发扫码刷新不会重复重连。对象客户端、历史和附件缓存跟随当前身份，避免继续使用旧身份票据。
- 保留 RC4 的有界握手、半开检测与心跳恢复；新增固定错误码诊断，不记录令牌、证明或带凭据 URL。VPN 是否构成用户原始故障的触发因素仍需现场复现，不能仅凭现象下结论。
- 电脑换网后可通过公网刷新 LAN 地址；云端不可达时，已配对手机可扫描带身份的安全地址定位码，再经加密验证恢复地址。定位码不提供授权，陌生手机不能借此绕过首次配对。

## 升级影响与安全边界（必读）

- 原公网节点身份、云端绑定和 DSH 会话正常保留，通常不需要删除节点或重新配对。
- **首次升级会将 LAN 授权绑定到当前公网身份，轮换 LAN 令牌并清理旧 LAN 绑定 / 待配对码。** 新体验小程序通过原有公网加密连接重新取得授权；旧小程序缓存的明文 LAN 授权可能失效，应走公网或升级客户端。不要把 RC4 的“LAN 令牌不变”作为 RC5 保证。
- 对应小程序 `1.6.5-trial.5` 不再使用旧插件的明文 LAN 或明文首次配对。v1.5.5 已公网配对用户仍可公网连接；需要安全 LAN 时升级插件。旧 LAN-only 用户需升级并完成安全配对。
- 公共 Wi-Fi 的设备隔离、防火墙、认证门户，以及封锁出站 HTTPS/WebSocket 的 VPN / 网络策略，不能由插件绕过。LAN 不通时尝试公网；两条路径都被网络阻断时会失败，不通过取消身份校验来强行连通。
- 云端 1.2.5 只增加脱敏诊断，不要求用户升级客户端，不迁移配对数据；本插件预览不进入正式推荐清单。

## RC5 验证范围

- 发布前执行构建、制品检查、完整插件测试、生产依赖审计；小程序 60 项单测及搭配 RC5 的 13 项跨端测试覆盖真实代码的加密、认证失败、重放、身份隔离及隧道行为。
- 本轮同源候选在 Windows Node 24.15、macOS Node 25.9、Ubuntu Node 22.22 各跑 7 项连接存活回归和 5 项加密 / 隧道测试；使用临时 loopback 环境，不冒充真实微信运行时或 RC5 全局安装认证。
- RC4 继承的三 DSH 版本 × 三 x64 主机矩阵见下方历史记录。**RC5 标签包尚未逐格重新安装真机验收**；ARM64、未来 DSH、服务管理器、长期弱网、陌生 Wi-Fi 迁移和 WebUI 实际升级仍需继续验收。

## 安装本预览

先正常结束任务并停止目标实例，保留原 HOME / profile，固定安装标签（非 web profile 使用原名称）：

```bash
npm exec --yes --package=pnpm@11 -- dsh plugin --profile web add github:martinbear1/dsh-wechat-remote#v1.6.0-rc.5
```

按原方式重启；WebUI 使用启动时打印的认证 URL。安装前备份原 profile；如需回退，先验证旧插件与当前 DSH 兼容，并保留新产生的会话，不恢复陈旧数据库。本次发布不自动更新任何用户电脑。

---

# RC4 历史发布记录（不是 RC5 新增验收）

**多版本 · Windows / macOS / Linux · 安全更新功能预览。Pre-release，非正式稳定版，不合并 main，不替换 Latest v1.5.5。**

从稳定 v1.5.5（`12bfe41bd289689d620a9f517ef1b92e6126d034`）整理本轮开发，不从历史 rc.3 叠加未知改动。此前两个 `preview.20260905.1/.2` 仅为升级起点/终点测试包，现已撤下并本地归档，RC4 作为后续功能预览基线。

## 功能与改动

1. **宿主版本适配**：读取实际 Web 服务端口，适配自定义端口、profile、DSH_HOME；在插件侧归一 RPC、权限读写、历史和实时事件差异，LAN / 公网共用边界。DSH 版本表不下沉到小程序。
2. **三主机与身份隔离**：Windows、macOS、Linux 的真实主机信息、私网地址、动态端口、目录与挂载卷；保留原 profile 状态路径和节点身份，不以主机名混淆多个实例。历史/图片/工作区仍以 DSH 为事实源。
3. **公网恢复**：修复心跳与半开连接处理，握手/重连有界，显式停止后不复活连接；重连不更换身份。小程序节点“公网离线 / LAN 待检测”文案是对应客户端改动。
4. **兼容判断**：主机与云端共用规则，按准确 DSH 版本、系统、架构选择已验证的兼容插件，不盲目追逐最大版本。应升级插件时不误导升级 DSH；未知版本不强制降级。
5. **旧 v1.5.5 用户过渡**：新小程序不依赖旧插件检查 API，可以从可靠元数据检查。正式适配目标发布后提示首次在原 HOME / profile 手工升级，不要求删除节点或重新配对。
6. **WebUI 一键更新**：设置 → 微信连接 → 插件更新与兼容。检查、确认、进度、重启后恢复。只有本机 loopback、当前 WebUI Origin/Host 的受控入口可发起；LAN 门和远程加密通道不能安装。
7. **更新事务安全**：限定本项目 GitHub 资产；校验 SHA-256、大小、tar 路径/类型；隔离暂存、禁用安装生命周期脚本、保留其他依赖；保存会话、备份 profile、重启原入口，再核验身份、配置和历史。失败尝试回滚，不能确认则保留记录并提示人工处理。

## 兼容状况与证据分级

下表为**同源开发候选已经执行的实机自动基础回归**，不是把历史记录改称 RC4 标签包已逐格重测：

| DSH | Windows x64 | macOS Intel x64 | Linux Ubuntu x64 |
| --- | --- | --- | --- |
| 0.1.1-rc.1 | 通过 | 通过 | 通过 |
| 0.1.1-rc.2 | 通过 | 通过 | 通过 |
| 0.1.2-rc.1 | 通过 | 通过 | 通过 |

- 基础矩阵 9/9；隔离合成节点更新事务矩阵 27/27（各组合的暂存失败保护、成功升级、启动失败回退）。不等同于所有真实用户安装环境。
- 用户对本轮多主机/多版本节点反馈 LAN / 公网基础对话和流式整体通过，权限切换反馈恢复；没有宣称所有审批、附件、弱网交互逐格验收。
- Windows / DSH 0.1.2-rc.1 已用产品入口从真实 GitHub 下载升级，核验身份/配置/历史与重启后真实模型流式。用户实际点击 WebUI 更新后，该测试实例完成、WebUI 恢复。
- 本次发布前复跑构建、制品检查、完整插件测试、更新策略与安全测试、生产依赖审计；并核对 tgz 与源码标签的一致性。资产 SHA 随发布提供。
- 对应小程序 v1.6.5 候选从 v1.6.4 稳定基线整理；旧客户端协议仍保留，但旧客户端没有新增提醒页面。

## 未认证边界

- 上表三个 DSH 都是 RC；不声明未列 RC、未来版本或整个 semver 区间受支持。
- ARM64 / Apple Silicon、Windows ARM、容器、Electron 包装、systemd / launchd / Windows 服务等未认证一键更新。
- 自动更新当前要求 x64、普通 Node CLI、可验证的原启动入口、独立可写 profile、可定位的 pnpm 11。共享 HOME、多进程同 profile、断电、跨用户安装和长期弱网仍待专项测试。
- 任务运行中拒绝切换；不会升级/降级 DSH 本体。安装方式不明时只提供手工途径，不猜 PID 强杀。
- RC4 发布包完整三主机安装/升级矩阵和体验版真机验收仍需继续；历史候选证据不自动升级为正式认证。
- Claude / 其他 Agent 类型尚未实现，本次先固定 DSH 兼容基础。

## 安装与恢复

仅供主动参加预览的用户。先结束任务、正常停止目标 DSH；保留原 HOME、profile、端口、身份文件，必要时自行备份。非 web profile 替换为原名称：

```bash
npm exec --yes --package=pnpm@11 -- dsh plugin --profile web add github:martinbear1/dsh-wechat-remote#v1.6.0-rc.4
```

按原方式重启 DSH。新版 WebUI 首次访问使用 DSH 启动输出的认证 URL；裸 localhost 可能提示 authentication required，不要关闭认证。正常保留原节点，无需重新扫码。

旧 profile 若提示 pnpm store / 大版本不一致，应确认原安装方式，不删除 HOME、store、工作区或会话绕过错误。需要回退稳定 v1.5.5 时，先确认当前 DSH 与旧插件兼容，再在原 HOME / profile 停止实例并固定安装对应标签；不要自动降级 DSH 或覆盖新产生的数据。

## 更新通道与三端状态

- 本包不会进入正式升级推荐。默认只选兼容 stable；安装 RC 不等于订阅 preview。
- 预发布更新需管理员同时设置 `HARNESS_REMOTE_UPDATE_CATALOG` 本地可信清单与 `HARNESS_REMOTE_UPDATE_CHANNEL=preview`。清单须填写精确 URL、SHA-256、字节数和组合。
- 云端 v1.2.3 正式检查接口已上线，初始正式清单没有新插件目标。看不到 RC4 升级提示是预期隔离，不是检查故障。
- 小程序 v1.6.5 只准备体验版，不提交审核或正式上线。插件正式发布仍需后续验收与明确放行；main / Latest 本次不变。

## 隐私

远程会话保持端到端加密，云端不持有模型凭据或会话明文。兼容检查只发送类型/版本/系统/架构，服务仍可见来源 IP。`<DSH_HOME>/harness-remote-updates/` 备份包含私密配置，不上传或发送给他人，稳定前勿删除。
