# v1.7.2 · 配对修复与升级兼容优化

- 修复部分已发布小程序扫码时提示“公网中继地址无效”的问题。
- 重新生成二维码时更新配对凭据，避免新手机扫描已经使用过的二维码而配对失败。同账号重新扫码保留原节点，无需删除重建。
- 改善 DSH 先升级后的插件更新：从本版起，后续已验证的兼容插件不再因旧版本列表限制而无法一键更新。新增 DSH `0.1.5-rc.1` 兼容。
- 配合新版小程序，改善文件附件、交付物、会话历史和流式回复的兼容性，以及每轮输出速度与耗时的显示。

升级前请结束运行中的任务；升级保留已有配对与会话，不修改 DSH 本体版本。新手机配对时，请在升级后的电脑 WebUI「设置 → 微信连接」重新生成二维码，不要继续使用旧二维码截图。本版不包含换机后免扫码自动找回节点。

支持 DSH `0.1.1-rc.2` / `0.1.2-rc.1` / `0.1.5-rc.1`，Windows / macOS Intel / Linux x64。可通过 WebUI 检查更新，或运行：

```sh
npx -y dsh-wechat-remote@latest
```

若旧插件 `1.7.1` 在 DSH `0.1.5` 上不能点击更新，请先运行上述命令过渡一次，无需降低 DSH 版本或重新配对。未验证的未来 DSH 版本和启动方式不会被盲目放行。
