# Harness Remote 微信版 bundle 层：单包双角色（微信认证网关 + 浏览器配对按钮）。
# 不替换 DSH/WebUI 的官方 directory-picker；小程序的远程目录浏览由本包
# Host-only Typert Remote 服务提供，不向 WebUI 注入目录 UI。
- insert:
    - id: gate
      name: '@harness-remote/dsh-wechat-remote'
