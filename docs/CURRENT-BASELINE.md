# 插件与安装器当前基线

## 2026-09-15：1.7.6 已正式发布 GitHub 与 npm

- 发布源码 `a6ebbc26e6736c15babece11438d1a529e30aed5`，固定标签 `v1.7.6`；GitHub Latest、npm `dsh-wechat-remote@latest` 和内嵌插件均为 `1.7.6`。本版本继承 RC 的任务通知，不使用早先被取消的稳定版旁支；旧 RC 标签与附件保持不变。
- GitHub 两个归档及 npm 注册表安装器均重新下载核验：插件 9549941 字节、SHA256 `ddd14140e93eaf1704dcca7380b1f99d8243089c2be337eaf94d2e87de4a0ff5`；安装器 18505422 字节、SHA256 `9d8e5a509ba39dee880a52af07fd8e9f7f1cf36f5b8073c245d123602eeb1f87`。内嵌插件与 GitHub 独立包逐字节一致。
- 三系统真实 npx 首装、重复安装及再次启动通过；Windows x64、macOS Intel、Linux x64、Linux ARM64 原生升级、WebUI 更新/重启及故障回退通过，配对与会话保持不变。TV 浏览器已恢复，原 ARM 节点 PID 未变；未替用户升级已有 Windows/Mac/Ubuntu 节点。完整记录见 [安装与升级审阅](INSTALLER-1.7.6-AUDIT.md)。
- 源码 main、发布标签、前后备份分支已推送；全量 bundle 经独立 bare 仓库恢复及 fsck 检查。发布回读、安装/回退报告和 bundle 保存于 `E:/agent remote/compat-artifacts/plugin-release-1.7.6-20260915/`。本节为发布回执，不移动构建源码标签。
- **云端已于 2026-09-16 00:03 完成切换**：用户补充确认允许既有通知服务联动重启后，1.2.13 在 `/opt/harness-remote-relay/releases/1.2.13-90d3b25` 生效，GitHub 同号发布。中继/通知健康、通知代码和配置不变、非 root 权限均验证通过；数据库未回滚。公网目录已提供稳定插件 1.7.6，未登记 DSH/CPU 的更新判断已生效。旧插件冻结的本地限制不能由云端删除，受影响旧 ARM 用户仍需用新版 npm 命令过渡一次。此前自动回退经过保留在云端基线历史中。

## 2026-09-15：1.7.6 正式发布准备

- 最终发布路线由用户明确改为从 `v1.7.6-rc.1` 继续，保留任务通知，合入三系统 npm/npx 宿主发现、POSIX 入口规范化、架构/DSH 测试记录与更新准入解耦及候选异常退出回退修复。插件、npm、GitHub 统一 `1.7.6` 正式 latest。
- 早先“仅 npm 1.7.6 内嵌 1.7.5”及“从 1.7.5 单独做稳定修复”的提案被后续指令替代，均未发布；不使用 `release/1.7.6-stable` 工作树。旧 RC 标签及附件保留。
- 三系统 npx 首装/重复执行/再次启动、全量原有测试及通知回归通过。Windows x64、macOS Intel、Linux x64、Linux ARM64 的最终同包原生升级、WebUI 更新/重启和故障回退均通过，会话与配对不变；ARM 原生孤儿锁恢复有确切子进程归属证明。发布回读结果另行追加，准备记录不代表已发布。
- 详见 [安装与升级审阅](INSTALLER-1.7.6-AUDIT.md)。本次不上传小程序、不改已有 Windows/Mac/Ubuntu 节点；云端 1.2.13 仅更新策略和发布目录。

## 2026-09-15：GitHub 已发布 1.7.6-rc.1 预览版

- 通知研究分支完成审阅、清理并合并 main；发布源码 `e03113ebfe7b3fcb2729348b2a77a41aa0ca64fd`，标签 `v1.7.6-rc.1`。GitHub Release 标题同为 `v1.7.6-rc.1`，`prerelease=true`；main、标签及前后备份分支已推送。
- **仅 GitHub 预览发布，未发布 npm，未改任何 npm dist-tag**。实际回读 npm latest 与 GitHub Latest 均为 `1.7.5`；正式安装器和线上更新清单不自动选择本 RC。后续正式 GitHub/npm 同步更新须另获用户授权。
- 插件、安装器清单及内嵌插件统一 `1.7.6-rc.1`。插件包 9547344 字节，SHA256 `15f7a085bca98068d25c7fe2a1a139d69dbc2a56c090644f938131a28777d97b`；安装器包 18507755 字节，SHA256 `f641e09f2221bfc0679b925ec14015ae8a672c3680b08fbcbd6f040b80dbd219`。内嵌插件逐字节一致；内嵌目录是 preview，显式 RC 安装不受稳定目录排除预览的规则误拦。
- 完整构建、全量既有测试及 12 项通知、12 项安装器选择测试通过；配套小程序 `1.7.6-trial.1` 已上传并由用户确认设为体验版，云端 `1.2.12` 已生产部署。本次没有代替用户升级已有 Windows/Mac/Ubuntu 节点或触发一键更新。
- 三通道已提前准备：按订阅时小程序原生环境保存 developer/trial/formal，正式审核发布后自动使用 formal，不再手工切服务器配置；既有消息不改通道。恢复兼容及审阅结果见下方记录。
- 发布制品、GitHub 回读、npm 标签核验、全量 Git bundle 与独立恢复校验保存在 `E:/agent remote/compat-artifacts/notifications-release-20260915/`。本段为文档收尾提交，发布标签继续固定构建源码。

## 2026-09-15：统一 1.7.6-rc.1 任务提醒整合候选

- 从正式 main 1.7.5 整合通知研究分支，插件与内嵌安装器统一 1.7.6-rc.1；计划发布 GitHub 预览版，保留 GitHub/npm latest=1.7.5。此次未要求 npm RC 上架，不变更 npm 标签。
- 仅读取宿主事件的可选观察器，无待提醒任务时无轮询；永久拒绝终止追踪，普通会话、问答/审批和更新流程不依赖通知发送。移除单机研发注入脚本。
- 完整构建、制品校验、原有全量测试及通知 12 项测试通过；RC 安装器明确接受自身 preview，正式安装器不自动选择预览版。
- 三端版本通道自动识别已准备：小程序按实际微信环境登记，云端提前支持 developer/trial/formal，后续正常送审发布无需手工改云配置。详见 [整合审阅](NOTIFICATIONS-1.7.6-REVIEW.md)。
- 备份和制品目录 `compat-artifacts/notifications-release-20260915/`；实际发布结果完成后追加，此段仅为准备记录。


## 2026-09-14 已发布统一版本 1.7.5

- 发布源码 `161bbe80376a9f11eefeeace223c43068e7b2103`；GitHub Latest / Release 标题 `v1.7.5`、npm `dsh-wechat-remote@latest` / 包内插件均为 `1.7.5`。源码标签 `v1.7.5`、基线 `baseline/plugin-1.7.5-20260914` 和备份分支已推送 GitHub；旧包和标签不改写，收尾文档单独提交。
- GitHub 两个归档重新下载、npm tarball 回读与本地产物逐字节一致，内嵌插件相同。插件归档 9539981 字节，SHA256 `d5545651e80242d1782fb04e86aece5d0460db80bdf1cec7688429e2abaf9e6e`；npm 归档 18499653 字节，SHA256 `c1781f285c17520a09aa3ddbce6fd20685efa1e248975b98979719c828ca85e6`。
- 云端只热更新外置目录 `stable-plugin-1.7.5-20260914`，源码 `c4573c5`；既有条目/策略保持不变。云端 76 项测试、九个目录组合评估通过；不是九组新硬件端到端测试。服务继续运行 1.2.11，无重启、无数据库修改。
- 13:30 北京时间只读检查 Mac：当前插件 1.7.2 / DSH 0.1.5-rc.1 / macOS x64，原生更新门返回目标 1.7.5、`canInstall=true`、automatic、无活动更新。用户亲自进行 WebUI 一键更新；未替用户触发安装或重启，不把“可更新”写成“已升级”。
- 配套小程序 `1.7.1-trial.7` 源码 `10055c4` 已上传 830128 字节；微信后台设为体验版需用户完成，真机问答/审批仍待复测。
- 本版继承 1.7.3 的 CLI 安装策略及 1.7.4 的慢启动支持；还包含此前 main 上的更新 worker 健康等待 180 秒、终止候选进程的有界等待与回退失败诊断，不将这些误记为尚未发布。新业务修复是交互完成事件适配，见下方准备记录。
- 完整 Git bundle、独立镜像恢复/fsck、精确产物、测试、上传回执和公网回读统一存于 `compat-artifacts/plugin-release-1.7.5-20260914/` 及配套小程序目录；不含真实节点凭据。发布后文档备份不移动源码标签。

## 2026-09-14 统一版本 1.7.5 发布准备

- 用户明确授权 GitHub/npm 发布修复版，根插件、npm 安装工具、归档元数据、GitHub v1.7.5 同号；历史资产不改写。
- 修复 native Gateway 不向 responder 回送 cancel 导致问答/审批 pending 永久保留；成功原生回执与他端完成通知共用幂等收口。双端同步、重连、并发、错误重试与旧源迟到测试通过。详见 [1.7.5 修复记录](INTERACTION-LIFECYCLE-1.7.5.md)。
- 完整构建、verify、npm test，以及小程序 331 客户端 / 21 跨端测试通过。已验证最终双归档与内嵌插件字节、SHA、包内版本相符。原生包对比保留之前 main 中安装策略、慢启动/回退修复，没有回退到旧 1.7.2 源码。
- 发布回执、制品与备份在 `compat-artifacts/plugin-release-1.7.5-20260914/`；发布后补充 GitHub/npm/目录回读。用户改为亲自测试 Mac WebUI 一键更新，本轮不自动更新或重启真实 Mac，不更改其配对/历史。

## 2026-09-14 发布前状态与统一版本约定（历史）

- 当前 npm `dsh-wechat-remote@latest` 为 `1.7.4`；其内置插件仍是未改动的正式 `1.7.2`。已发布文件保持原样，不以标题改动冒充包内版本升级。
- `1.7.3` 解除 CLI 的版本/架构登记准入拦截，`1.7.4` 修复 ARM64 盒子慢启动超时。公网一行命令、自动重启、重复执行及制品核验见 [ARM64 验收记录](arm64-installer-tvbox-20260914.md)。
- GitHub 公开发布标题已统一为纯版本号 `v1.7.4`、`v1.7.3`、`v1.7.2` 等；原标签、附件与正文保留。旧预览草稿不删除、不改为公开发布。
- 用户明确要求后续整个插件端只有一条产品版本线：DSH 插件、npm 安装工具、仓库发布基线和 GitHub Release 同号；只更新安装工具也共同递增。下一次从未发布的 `1.7.5` 起同步包内版本，具体流程见 [统一版本规则](UNIFIED-RELEASE-VERSION.md)。本轮尚未构建或发布 `1.7.5`。
- 此次列表整理没有修改更新清单、现有配对或运行逻辑。GitHub Latest 暂保留现有插件 `v1.7.2`，统一的新包正式发布时一起推进。

## 1.7.2 发布基线（2026-09-11，历史记录）

- 已完成发布：源码 `2c618de`、GitHub Latest `v1.7.2`、npm latest `1.7.2`、生产目录 `stable-plugin-1.7.2-20260911`；三端隔离升级/回退、最终包下载和 SHA 校验均通过。详情见下方专项交接。
- 本版源码与安装器版本为 `1.7.2`；正式发布标识 `v1.7.2`。本次将 1.7.1 后研究分支中的功能代码整体纳入，不能再把资源/输入/过程/流式/计时适配当作“仅在未发布插件里的功能”。
- 专项交接表：[RELEASE-1.7.2-HANDOFF.md](RELEASE-1.7.2-HANDOFF.md)，逐项记录已纳入提交、模块/协议、小程序消费要求、验证及尚未实现范围。公开发布与清单回读结果在该文档单独记录。
- 小程序正式版不会因为插件发布而自动获得研发 UI；本次不上传小程序、不修改其代码，不升级或重启真实节点。云端仅准备增加兼容更新清单条目，不改服务代码或账号/节点数据库。
- 更新器不再把当前 DSH 版本与本地冻结列表比较：共享原生能力检查 + 可刷新的目标版本兼容清单，各司其职。未知未来版本不自动放行；发布经过验证的兼容目标后，不必为了让旧更新器认识宿主版本而手工先升级它。
- DSH 0.1.5-rc.1 三系统 x64 的隔离原生更新验证见专项交接；新增该正式安装组合。旧 1.7.1 的冻结限制无法由云清单消除，使用独立安装器一次过渡；不改 DSH 本体。
- 备份位置：`E:/agent remote/compat-artifacts/plugin-release-1.7.2-20260911/`；保留发布前完整 bundle、工作区差异、发布后完整 bundle、安装包和远端回读校验。下面均为历史记录，不覆盖本节版本。

## 配对兼容修复候选（2026-09-11）

- 基于本分支 `3c85185` 继续修复，插件/安装器候选版本 `1.7.2`。正式 `v1.7.1`（`f39e7a3`）已经是本分支祖先，保留两者之间的全部研发改动，不重复合并。
- 统一公网 QR v1 的 `relay` / `relayOrigin` 字段来源，修复已发布小程序的扫码入口兼容；显式生成二维码申请新的单次配对票据，保留原节点身份/账号归属；独立配对页不再自动刷新票据。
- 本次未修改小程序或云端运行代码，未发布 npm/GitHub Release，未安装或重启真实节点。候选包不代表线上已修复。
- 未改动的旧小程序、正式 1.7.0、体验 1.7.1-trial.3 代码已通过隔离 HTTP 配对回归；微信登录交换和配对后的连接由测试替代，仍需真实微信扫码验收。自动免扫码恢复尚未实现。
- 根因、验证、所保留研发功能及后续恢复边界见 [配对修复记录](PAIRING-COMPAT-20260911.md)。以下保留历史基线。

## 配套体验包三端备份（2026-09-11 15:30）

- 配套小程序 `1.7.1-trial.3` 侧边栏/工作区优化，插件运行代码相较 `b47606f` 无新增改动，版本仍为 `1.7.1`；仅增加本次备份记录。
- 新快照 `baseline/workspace-sheets-20260911-1530` / `backup/workspace-sheets-20260911-1530`；旧标签和正式主线不变。不发布 npm/GitHub Release，不重新安装或重启电脑插件。
- 三端完整 Git bundle、精确提交与恢复/远端验证清单位于 `E:/agent remote/compat-artifacts/git-baselines/workspace-sheets-20260911-1530/`。本次是代码备份，不包含在线配置、凭据或业务数据。

## 当前研发基线（2026-09-11）

- 当前研发包版本仍为 `1.7.1`，本批代码基础 `ad0b9d7`；DSH 0.1.5-rc.1 的资源/输入/过程/流式/计时等适配改动已纳入。
- 这是正式插件 1.7.1 之后的研发快照，不等同于 npm/GitHub 已发布版本；本批不重新安装或发布插件。
- 共同分支 `research/agent-resources-20260910`；本批标签 `baseline/research-agent-resources-20260911-0225`。正式主线与旧发布标签保持原指向。
- 配套三端、验证边界及恢复规则见 [三端研发基线](RESEARCH-BASELINE-20260911.md)。

## 以下为 2026-09-10 正式基线与验收的历史记录

以下内容保留追溯，不覆盖上面的研发状态，也不代表本轮重新核验公开渠道或生产服务。

更新：2026-09-10。

## 版本与发布

- DSH 插件 `1.7.1`，npm 安装器 `dsh-wechat-remote@1.7.1`。
- 已发布源码 `f39e7a3`，GitHub 正式标签 `v1.7.1`，npm latest 为 `1.7.1`，本轮公开渠道回读确认。
- 本轮仅补充基线说明，不变更运行代码、不覆盖发布资产、不再次发布 npm 包。
- 基线标签 `baseline/plugin-1.7.1-20260910`；原有 `v1.7.1` 不移动。

## 已实现

- Windows x64、macOS Intel、Linux x64；已声明兼容 DSH `0.1.1-rc.2` / `0.1.2-rc.1`。
- 统一安装/升级命令 `npx -y dsh-wechat-remote@latest`；随包提供 pnpm，无需用户另外安装全局 pnpm 或 Git，不升级 DSH 本体。
- WebUI 检查兼容更新、自动备份/安装/重启及失败恢复；保留既有身份、配对、配置和会话。
- 新旧 DSH 原生 API 适配、父子代理 SessionAddress、历史/轨迹/计划/目标及可选轮次用量/用时投影。
- DSH 结构转换留在插件边界；通用能力缺失时隐藏，不修改或修补用户历史文件。
- 安全局域网、端到端加密公网、附件与历史对象传输和断线恢复。

## 验证边界

- 本轮完整 npm test 通过；此前 Windows 公开 npm 冷缓存命令从 1.5.5 升到 1.7.1，以及 WebUI 同源更新 API 从 1.7.0 升到 1.7.1 均有成功记录。
- API 实测不描述为已人工点击按钮；未把全部平台、ARM64、任意进程包装器或所有网络环境标记为已验证。
- 公网支付属于小程序与云端，不要求插件因支付功能另发新版本。
- 通用能力合同不等于 Claude Code 适配器已开发；该节点类型尚未接入。
