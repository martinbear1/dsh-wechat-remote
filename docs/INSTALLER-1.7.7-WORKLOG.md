# 1.7.7 安装与更新修复：工作记录

## 范围和发布门槛

- 起点：正式 `main` / `527a98a2fade7eb288be6d35275e05d0dbfa740b`，不合入 ClawBot 研究分支。
- 分支：`fix/install-reliability-1.7.7`。
- 目标：插件与 npm 安装器同号 `1.7.7`，仅交付待审核候选，未经用户最终确认不发布、不推进生产更新目录。
- 不修改小程序/云端接口、配对、通知、支付或生产服务。
- 用户授权在儿童 Windows、MacBook、Ubuntu、ARM64 电视盒子清理 DSH 后真实安装测试；先确认精确路径与在途任务，保留无关应用和家庭服务。

## 已确认问题

1. Windows 临时 pnpm.cmd 将 Node 中文绝对路径写为 UTF-8；后台 cmd 按 936 读取时路径错误。外层 chcp 65001 不保证后台环境继承。英文路径对照成功，中文路径复现。
2. 深层 staging-home 改变本地依赖相对路径语义；当前以白名单限制其他插件来源并拒绝外部链接，未覆盖 DSH 官方允许的 Git / file / link 场景。
3. 原生安装失败仅给通用提示，不输出确切日志路径；包装子进程未保留退出码/信号，超时路径需要核对完整退出与回退顺序。
4. 部分 README 仍写旧版本/旧架构范围；官方原生安装备用入口尚未形成独立验收流程。

## 工作顺序

1. 复现测试与安装生命周期审阅。
2. 修复共享工具启动、日志与进程处理；覆盖中文、空格和 Windows 特殊字符，不修改系统代码页/全局 PATH。
3. 原目录调用官方安装，不以其他插件的来源或逐文件摘要作准入条件，不预改其他插件依赖；原生包管理器正常维护依赖及锁文件。不自动放开第三方构建脚本权限。备份仅用于恢复。
4. 检查宿主发现、首次初始化、并发/重复操作、原生退出和启动参数恢复。
5. 构建同一批候选制品并测试四台真实主机的 NPX/全局安装、首次安装、CLI 重装、WebUI 更新与重启、故障回退。
6. 补用户文档、原生安装备用方式、制品校验和测试记录；提交审核。

## 当前状态

- 已确认本地 main 与 origin/main 相同，独立修复 worktree 建立。
- 儿童 Windows 新地址 192.168.3.45 以原 192.168.3.33 主机密钥验证连接。四机只读盘点已完成，均无运行中的会话；Mac/Ubuntu/ARM 为全局 DSH 0.1.5-rc.1，儿童 Windows 为 NPX 同版本，插件均 1.7.6。
- 已移除 profile 搬迁安装、特殊来源白名单、他包 package.json 哈希准入、packageManager 强制重写。安装保留在原目录；完整 profile 备份不执行，只在失败时恢复到原路径。
- 工具独立复制到当前事务目录，防止自更新删除正在用的 pnpm；重启检查引用同一份已固定工具。
- 修复 Windows UTF-8 批处理中文路径与 PATH/Path 冲突；不修改系统代码页、全局 PATH、DSH 本体。
- 移除命令行必须含 web 字样的限制，实际 Web 服务能力检查保留，兼容 --profile web 的官方启动方式。
- 默认选择顺序：已运行实例握手优先；未运行时 PATH 默认 / npm 全局 / 当前项目或源码 / 唯一 NPX；仅缓存多份且无默认目标时询问。
- 安装错误包含退出码和日志位置；超时先结束本次安装进程树，不能证明退出时不并发回退。
- Windows 单元及全量插件回归通过；POSIX 特有测试在 Windows 跳过，待对应真机执行。真实本机 DSH 原生 add 已通过 1.7.6→候选 1.7.7 的临时 profile 安装测试（不是四机整套验收）。
- 构建了第一批 1.7.7 候选包，仍需完整真实首次安装、自动重启、混合依赖、故障恢复测试和文档整理，不可发布。
- main、ClawBot 分支、npm/GitHub latest、云服务、小程序保持原状。

## 2026-09-16 真机进展（未完成验收）

- 用户已再次确认四机测试授权，包含临时关闭电视浏览器并在测试后恢复。
- 第一批候选：Mac 与 Ubuntu 的现有全局 DSH 0.1.5-rc.1，实际从插件 1.7.6 升级到候选 1.7.7 并自动重启，通过节点身份与会话校验。
- Ubuntu 已按授权清除默认 DSH 安装、配置/配对/会话及 DSH 专用下载缓存，保留其他应用；重新以官方 NPX 安装 0.1.5-rc.1。关闭 DSH 后，用第二批候选完成首次插件安装，自动启动同一 NPX DSH，原生 session.list 正常。当前尚未重新配置 API Key/配对。
- 第二批候选补上停止状态直接原生安装、不必启动其他插件才能安装；同版本但运行未就绪时普通安装命令尝试修复；不确定安装进程仍存活时保留锁。
- 儿童 Windows NPX 现有 1.7.6 升级发现 ERR_PNPM_UNEXPECTED_VIRTUAL_STORE：旧版 staging-home 的路径仍记在 node_modules 元数据中。候选正确自动恢复 1.7.6，但升级未成功，不计通过。
- 为此补充窄范围恢复：仅收到包管理器布局不兼容错误时，使用官方 dsh plugin install 重建现有依赖布局，再 add 一次；不使用 --force、不删锁文件、不重写其他插件来源、不放开构建权限。回归测试已通过，第三批候选待真机验证。
- 电视浏览器 summer2026-kiosk.service 原状态 active（PID 302153），已停止，必须在测试后恢复。首次 root SSH 下切 martin 执行 npm 时因 cwd=/root 无权限退出，属于测试启动目录错误，未执行安装器；更正 cwd=/home/martin 后继续。
- 候选制品分别保存在 compat-artifacts/plugin-release-1.7.7-20260916/candidate-2、candidate-3，不是公开发布包。

## 后续验证

- 儿童 Windows 第三批候选升级通过：旧 staging 虚拟仓记录经官方 install 修复，1.7.6 → 1.7.7、自动重启、原有两条会话及配对校验通过。
- ARM 现有全局安装升级候选 1.7.7 通过；随后完整清理默认安装并重新用 NPX 安装 DSH，第四批候选首次安装/重启通过。全局干净安装仍在测试。
- Mac：干净 NPX + 运行中首次安装通过；重复执行正确提示无需更新；再次清理后，干净 npm 全局 + DSH 尚未启动时安装通过，实际 WebUI 与 session.list 已确认。
- 儿童 Windows：干净 NPX + 运行中首次安装通过，现已再次清理并重新 npm 全局安装；中文加空格 Node.exe 路径的真实安装正在验证。
- Ubuntu：第二轮干净全局 DSH 已安装，注入损坏候选测试首次失败恢复。第四批候选恢复器的 YAML 打包缺少 CJS 内置模块桥接导致恢复检查失败；原 profile 已恢复，但不计恢复成功。第五批仅修改恢复器打包方式，增加独立模块加载回归，再次真实验证中。
- Mac 的 WebUI 后续升级使用私人合成版本及精确下载映射（因为 1.7.7 尚未公开），生产更新检查/哈希/安装/重启代码均不替换。第一次测试夹具目录中的 DSH 版本字段不合规，目录被拒绝；修正夹具后更新成功，损坏版本回退仍在测试。不得将这项记录称为公网 1.7.7 发布下载已验证。
- 第四批候选全量 npm test 通过。main、其他两端与生产服务未变。
- 第五批：Ubuntu 首次失败恢复的真实复测通过，恢复后原生会话检查通过，再执行普通安装成功。Mac、Ubuntu、儿童 Windows（中文 Node 路径）WebUI 更新及损坏版本自动回退均通过，并恢复到未修改的 1.7.7 候选。
- Mac 人为保留未完成的更新验证标记，确认 HTTP 503 真实出现；再次执行普通安装（无 --repair），自动识别未就绪、重装、重启，恢复 HTTP 正常及同版本 1.7.7，通过。
- 本机真实 DSH/pnpm 临时 profile 集成：同时存在 file:../fixture-bundle 与 link:../fixture-linked 时安装及升级通过，来源不变，链接仍指向原目录，DSH 正常登记第三方 bundle。该项是原生依赖共存集成测试，不冒称某个外部插件的全部功能均已验证。
- Mac 的 18 个宿主发现用例全部通过（含 POSIX 原生链接），profile 备份/相对链接恢复/旧布局重建/异常进程树清理通过。Windows 全量回归、verify、更新专项通过。

## 发布前必须完成

- 完成四机测试、错误恢复及最终制品校验，并由用户最终审核。当前不是可直接发布结论。
- GitHub Release 同时上传带版本号附件与固定别名 `harness-remote-dsh-wechat-remote.tgz`，两者字节一致；固定别名供原生 latest 命令使用。该地址当前尚未公开生效，发版后必须下载核验，不能提前声称可用。
- 更新目录仍使用带版本号附件及 SHA-256，不使用可变的 latest 别名；本轮不擅自部署云端。
- 恢复电视浏览器原 active 状态，清理儿童电脑临时启动任务，恢复正常 Node 入口，说明四机干净安装后需要重新配置 API Key/配对。

## 最终候选收尾

- ARM 原生全局首装及 WebUI 更新/坏包回退完成，会话与身份校验通过，恢复真实候选 1.7.7。首次 ARM 会话夹具在更新前被测试用 SIGTERM 打断异步保存，已保留失败报告并修正测试时序，不能把这次夹具问题说成产品更新丢会话。
- 电视浏览器已恢复 active/running，DSH 健康正常。儿童 Windows 中文测试 Node 副本已移除，临时任务定义已移除，后续另加纯 NPX WebUI 复核轮次，须再次清理其测试任务。
- 最终 review-candidate 与已测五号候选的插件运行代码逐字节一致；安装器仅帮助文本、文档和制品元数据不同。再次全量 npm test/verify 通过，内嵌插件与独立包及固定别名相同。
- 已准备正式安装兜底文档和详细交付审阅，尚未 npm/GitHub 发布、main 合并或云目录更新。
- 儿童 Windows 纯 NPX 追加轮次通过前向更新、重启、故障回退、会话读取。测试清场命令行路径匹配曾退出（不是产品更新失败），已改为真实文件路径匹配并完成独立候选还原，原记录保留。最终 PID 7548、候选 1.7.7、两条空测试会话；临时任务再次全部移除，正常 Node 路径，保持 NPX 方式。四机无需继续暂停。
- 最终制品只登记本次真机 DSH 0.1.5-rc.1 证据，不照搬早期版本的完整测试矩阵；这些记录不构成安装白名单。详细范围与限制以 INSTALLER-1.7.7-AUDIT.md 为准。
