# Agent远程管理助手 · DSH 插件安装器

首次安装和升级旧插件使用同一个命令：

```sh
npx -y dsh-wechat-remote@latest
```

需已安装 DSH，使用 Node.js 22.13 或更高版本。安装工具由包依赖自动提供，无需另装 pnpm 或 Git。

安装器仅选择适配当前 DSH 和主机的正式插件，保留原配置和配对。不会升级 DSH 本体。

开发候选尚未发布；不要在正式指引中启用，直到 npm 正式发布及真实安装验收完成。
