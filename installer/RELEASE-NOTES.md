# 安装器 1.7.4

- 修复部分 ARM64 Linux 设备因 DSH 启动较慢而安装超时的问题。
- 改善首次启动时的安装等待，以及异常恢复时等待进程正常退出的处理。

这是安装工具更新，安装的正式插件仍为 1.7.2，不改变插件内的一键更新功能。已正常使用的用户无需重装。

此前遇到安装超时的用户，在安装了 DSH 的电脑运行：

```sh
npx -y dsh-wechat-remote@latest
```
